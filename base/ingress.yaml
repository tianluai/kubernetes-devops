# Hostnames are overridden per overlay (staging). Prod uses these defaults.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tianluai-ingress
  labels:
    app.kubernetes.io/part-of: tianluai
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  # TLS: create Secret `tianluai-tls` in the namespace (see README — Cloudflare Origin CA or cert-manager).
  tls:
    - hosts:
        - ai.tianlu.tech
        - ai-api.tianlu.tech
      secretName: tianluai-tls
  rules:
    - host: ai.tianlu.tech
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tianluai-web
                port:
                  number: 80
    - host: ai-api.tianlu.tech
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tianluai-api
                port:
                  number: 80
