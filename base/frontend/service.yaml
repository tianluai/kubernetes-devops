apiVersion: v1
kind: Service
metadata:
  name: tianluai-web
  labels:
    app: tianluai-web
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: tianluai
spec:
  type: ClusterIP
  selector:
    app: tianluai-web
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
