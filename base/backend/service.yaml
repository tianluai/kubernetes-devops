apiVersion: v1
kind: Service
metadata:
  name: tianluai-api
  labels:
    app: tianluai-api
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: tianluai
spec:
  type: LoadBalancer
  selector:
    app: tianluai-api
  ports:
    - port: 80
      targetPort: 3001
      protocol: TCP
      name: http
