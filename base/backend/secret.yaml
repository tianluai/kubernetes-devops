apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  labels:
    app: tianluai-api
    app.kubernetes.io/component: backend
type: Opaque
stringData:
  MONGODB_URI: "REPLACE_IN_OVERLAY"
  CLERK_JWT_KEY: "REPLACE_IN_OVERLAY"
  CORS_ORIGIN: "REPLACE_IN_OVERLAY"
