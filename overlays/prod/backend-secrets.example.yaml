# Copy to a file outside Git, fill values, then:
#   kubectl apply -f backend-secrets.local.yaml -n tianluai-prod
# MONGODB_URI must start with mongodb:// or mongodb+srv://
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: tianluai-prod
  labels:
    app: tianluai-api
    app.kubernetes.io/component: backend
type: Opaque
stringData:
  MONGODB_URI: "REPLACE_WITH_PROD_MONGODB_URI"
  AUTH_JWT_SECRET: "REPLACE_WITH_PROD_AUTH_JWT_SECRET"
  # Comma-separated v1:<base64-32B>,v2:... — see kubernetes-devops README "Workspace API keys"
  CREDENTIALS_ENCRYPTION_KEYS: "v1:REPLACE_WITH_OPENSSL_RAND_BASE64_32"
  SENTRY_DSN: ""
