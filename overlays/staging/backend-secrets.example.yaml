# THIS FILE IS COMMITTED (template only). Replace placeholders in a COPY named *-secrets.local.yaml
# (gitignored) — never commit real values. Prefer GitHub Environment secrets + deploy.yml for sync.
# Local apply: kubectl apply -f backend-secrets.local.yaml
# MONGODB_URI must start with mongodb:// or mongodb+srv://
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: tianluai-staging
  labels:
    app: tianluai-api
    app.kubernetes.io/component: backend
type: Opaque
stringData:
  MONGODB_URI: "REPLACE_WITH_STAGING_MONGODB_URI"
  AUTH_JWT_SECRET: "REPLACE_WITH_STAGING_AUTH_JWT_SECRET"
  SENTRY_DSN: ""
