# Committed template; copy to frontend-secrets.local.yaml (gitignored), fill values, kubectl apply.
# Prefer GitHub Environment secrets + deploy.yml.
apiVersion: v1
kind: Secret
metadata:
  name: frontend-secrets
  namespace: tianluai-staging
  labels:
    app: tianluai-web
    app.kubernetes.io/component: frontend
type: Opaque
stringData:
  AUTH_SECRET: "REPLACE_WITH_STAGING_AUTH_SECRET"
  AUTH_GOOGLE_ID: "REPLACE_WITH_STAGING_AUTH_GOOGLE_ID"
  AUTH_GOOGLE_SECRET: "REPLACE_WITH_STAGING_AUTH_GOOGLE_SECRET"
  AUTH_JWT_SECRET: "REPLACE_WITH_STAGING_AUTH_JWT_SECRET"
